import { Placeholder } from "../shared/Placeholder";

export function RnaseqView() {
  return (
    <Placeholder
      title="Bulk RNA-seq — DESeq2"
      subtitle="Upload a counts matrix + sample metadata, run DESeq2 differential expression, and render volcano / MA / PCA / heatmaps. (Coming in Phase 2d.)"
    />
  );
}
