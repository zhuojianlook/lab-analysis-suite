import { Placeholder } from "../shared/Placeholder";

export function QpcrView() {
  return (
    <Placeholder
      title="qPCR — ΔΔCt Analysis"
      subtitle="Upload qPCR + plate-map files, compute fold-change, t-test / ANOVA+Tukey, and render ggplot2 bar charts. (Coming in Phase 2b.)"
    />
  );
}
